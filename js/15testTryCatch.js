try {
  console.log(`i : `, i);
} catch (error) {
  //console.error("Erreur attrapée : " + error.message);
  console.log(`tout va bien, i n'est pas définie mais ce n'est pas un pb !`);
}

console.log(`Hello`);

